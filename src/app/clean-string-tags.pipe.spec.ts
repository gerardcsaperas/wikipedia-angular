import { CleanStringTagsPipe } from './clean-string-tags.pipe';

describe('CleanStringTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanStringTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
